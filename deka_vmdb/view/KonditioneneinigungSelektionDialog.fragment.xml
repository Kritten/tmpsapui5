<core:FragmentDefinition
	xmlns="sap.m"
	xmlns:core="sap.ui.core">
	<TableSelectDialog
		noDataText="Keine Daten vorhanden"
		title="Auswählen"
		search="onSelectDialogSearch"
		confirm="onSelectDialogConfirm"
		items="{anlRbg>/data}" >
		
		<columns>
			<Column>
				<header>
					<Text text="ID" />
				</header>
			</Column>
			<Column>
				<header>
					<Text text="Beschreibung" />
				</header>
			</Column>
			<Column visible="true">
				<header>
					<Text text="Nachhaltige Miete" />
				</header>
			</Column>
		</columns>
		
		<ColumnListItem>
			<cells>
				<Text text="{anlRbg>id}" />
				<Text text="{anlRbg>descr}" />
				<Text text="{anlRbg>nachhaltigeMiete}" />
			</cells>
		</ColumnListItem>

	</TableSelectDialog>
</core:FragmentDefinition>