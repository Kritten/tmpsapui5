<core:View 
    xmlns="sap.m"
    xmlns:f="sap.ui.layout.form"
    xmlns:l="sap.ui.layout"
    xmlns:core="sap.ui.core"
    xmlns:mvc="sap.ui.core.mvc"
	controllerName="ag.bpc.Deka.controller.VermietungsaktivitaetDetails" >
	
	<Page id="idVermietungsaktivitaetDetails" title="{i18n>VERM_DET_TITLE}" class="sapUiFioriObjectPage" showNavButton="true" navButtonPress="onBack" >

        <headerContent>
			<Button icon="sap-icon://edit" text="{i18n>BEARBEITEN}" visible="{= ${form>/modus} === 'show' }" press="onBearbeitenButtonPress" />
		</headerContent>
                
		<content>
            
            <FlexBox>
                <items>
                    <FlexBox>
                        <items>
                            <ObjectHeader title="{i18n>VERMIETUNGSAKTIVITAET} {form>/vermietungsaktivitaet/VaId}" >
                                <attributes>
                                    <ObjectAttribute title="{i18n>BUCHUNGSKREIS}" text="{form>/vermietungsaktivitaet/Bukrs}" />
                                    <ObjectAttribute title="{i18n>WIRTSCHAFTSEINHEIT}" text="{form>/vermietungsaktivitaet/WeId}" />
                                    <ObjectAttribute title="{i18n>BEZEICHNUNG}" text="{form>/vermietungsaktivitaet/Bezeichnung}" />
                                </attributes>
                            </ObjectHeader>
                        </items>
                        <layoutData>
							<FlexItemData growFactor="1" />
						</layoutData>
                    </FlexBox>
                    
                    <FlexBox justifyContent="End">
                        <items>
                            <List items="{form>/vermietungsaktivitaet/VaToMap}" growing="true" growingThreshold="10" class="sapUiContentPadding" backgroundDesign="Transparent" showSeparators="None" >
                                <StandardListItem 
                                    type="Active"
                                    title="{form>VaId}"
                                    icon="{= ${form>Aktiv} ? 'sap-icon://status-positive' : 'sap-icon://status-inactive' }" press="onMappingPressed">
                                    <customData>
                                        <core:CustomData key="state" value="{= ${form>Aktiv} ? 'active' : 'inactive' }" writeToDom="true" />
                                    </customData>
                                </StandardListItem>
                            </List>
                        </items>
                        <layoutData>
							<FlexItemData growFactor="1" />
						</layoutData>
                    </FlexBox>
                </items>
            </FlexBox>


            <Table id="mietflaechenangabenTable" mode="{= (${form>/modus} === 'new') || (${form>/modus} === 'edit') ? 'MultiSelect' : 'None' }" items="{form>/vermietungsaktivitaet/VaToOb}">

                <headerToolbar>
                    <Toolbar>
                        <Title text="{i18n>MIETFLAECHENANGABEN}" level="H2"/>
                        <ToolbarSpacer />
                        <Text text="{form>/viewsettings/zeitspanneSelected/text} | {form>/viewsettings/waehrungSelected/text} | {form>/viewsettings/flaecheneinheitSelected/text}" />
                        <Button icon="sap-icon://action-settings" press="handleTableSettingsButton" />
                    </Toolbar>
                </headerToolbar>
                
                <columns>
                    <Column minScreenWidth="Tablet" demandPopin="true">
                        <Text text="{i18n>MIETFLAECHE}" />
                    </Column>
                    <Column minScreenWidth="Tablet" demandPopin="true">
                        <Text text="{i18n>BEZEICHNUNG}" />
                    </Column>
                    <Column minScreenWidth="Tablet" demandPopin="true">
                        <Text text="{i18n>NUTZUNGSART}" />
                    </Column>
                    <Column minScreenWidth="Tablet" demandPopin="true">
                        <Text text="{i18n>NUTZUNGSART} {i18n>ALTERNATIV}" />
                    </Column>
                    <Column minScreenWidth="Tablet" demandPopin="true">
                        <Text text="{i18n>HAUPTNUTZFLAECHE}" />
                    </Column>
                    <Column minScreenWidth="Tablet" demandPopin="true">
                        <Text text="{i18n>MIETFLAECHE} {i18n>ALTERNATIV}" />
                    </Column>
                    <Column minScreenWidth="Tablet" demandPopin="true">
                        <Text text="{i18n>MIETFLAECHENSPLIT}" />
                    </Column>
                    <Column minScreenWidth="Tablet" demandPopin="true">
                        <Text text="{i18n>MARKTUEBLICHE_MIETE} in {form>/viewsettings/waehrungSelected/text} / {form>/viewsettings/flaecheneinheitSelected/text}" />
                    </Column>
                    <Column minScreenWidth="Tablet" demandPopin="true">
                        <Text text="{= ((${form>/modus} === 'new') || (${form>/modus} === 'edit')) ? '*' : '' }{i18n>ANGEBOTSMIETE} in {form>/viewsettings/waehrungSelected/text} / {form>/viewsettings/flaecheneinheitSelected/text}" />
                    </Column>
                    <Column minScreenWidth="Tablet" demandPopin="true">
                        <Text text="{i18n>GRUNDAUSBAUKOSTEN} in {form>/viewsettings/waehrungSelected/text} / {form>/viewsettings/flaecheneinheitSelected/text}" />
                    </Column>
                    <Column minScreenWidth="Tablet" demandPopin="true">
                        <Text text="{i18n>MIETERAUSBAUKOSTEN} in {form>/viewsettings/waehrungSelected/text} / {form>/viewsettings/flaecheneinheitSelected/text}" />
                    </Column>
                </columns>
                
                <items>
                    <ColumnListItem vAlign="Middle">
                        <cells>
                            <Text text="{form>Bukrs}/{form>WeId}/{form>MoId}" />
                            <Text text="{form>Bezeichnung}" />
                            <Text text="{form>Nutzart}" />
                            <Select selectedKey="{form>NutzartAlt}" items="{path: 'form>/alternativeNutzungsarten', templateShareable: true}" enabled="{= (${form>/modus} === 'new') || (${form>/modus} === 'edit') }">
                                <items>
                                    <core:Item key="{form>key}" text="{form>text}" />
                                </items>
                            </Select>
                            <Text text="{form>Hnfl}" />
                            <Input value="{form>HnflAlt}" type="Number" editable="{= (${form>/modus} === 'new') || (${form>/modus} === 'edit') }" />
                            <CheckBox selected="{form>MfSplit}" editable="{= (${form>/modus} === 'new') || (${form>/modus} === 'edit') }" />
                            <Text text="{form>NhMiete}" />
                            <Input value="{form>AnMiete}" type="Number" editable="{= (${form>/modus} === 'new') || (${form>/modus} === 'edit') }" />
                            <Input value="{form>GaKosten}" type="Number" editable="{= (${form>/modus} === 'new') || (${form>/modus} === 'edit') }" />
                            <Input value="{form>MaKosten}" type="Number" editable="{= (${form>/modus} === 'new') || (${form>/modus} === 'edit') }" />
                        </cells>
                    </ColumnListItem>
                    
                </items>
            </Table>
            
            <MessageStrip id="mietflaechenangabenErrorBox" type="Error" class="sapUiSmallMargin" visible="false" />

            <FlexBox class="sapUiSmallMargin" visible="{= (${form>/modus} === 'new') || (${form>/modus} === 'edit') }">
            
                <items>
                    <FlexBox>
                        <items>
                            <Button text="{i18n>KONDITIONENEINIGUNG_HINZUFUEGEN}" icon="sap-icon://sys-add" press="onKonditioneneinigungHinzufuegenButtonPress" class="sapUiSmallMarginEnd" />
                            <Button text="{i18n>KONDITIONENEINIGUNG_ENTFERNEN}" icon="sap-icon://sys-minus" press="onKonditioneneinigungLoeschenButtonPress" class="sapUiSmallMarginEnd" />
                        </items>
                        <layoutData>
							<FlexItemData growFactor="1" />
						</layoutData>
                    </FlexBox>
                    
                    <FlexBox justifyContent="End">
                        <items>
                            <Button text="{i18n>VERTEILEN}" icon="sap-icon://overview-chart" press="onAusbaukostenVerteilenButtonPress" />
                        </items>
                        <layoutData>
							<FlexItemData growFactor="1" />
						</layoutData>
                    </FlexBox>
                
                </items>
            </FlexBox>
                    
            <f:SimpleForm
                title="{i18n>GEMEINSAME_ANGABEN}"
                minWidth="1024"
                maxContainerCols="2"
                editable="true"
                layout="ResponsiveGridLayout"
                labelSpanL="3"
                labelSpanM="3"
                emptySpanL="0"
                emptySpanM="0"
                columnsL="1"
                columnsM="1" >

                <f:content>
                    
                    <Label text="{i18n>VERMIETUNGSART}" required="{= (${form>/modus} === 'new') || (${form>/modus} === 'edit') }" />
                    <Select id="vermietungsart" selectedKey="{form>/vermietungsaktivitaet/Vermietungsart}" items="{form>/vermietungsarten}" enabled="{= (${form>/modus} === 'new') || (${form>/modus} === 'edit') }">
                        <items>
                            <core:Item key="{form>key}" text="{form>text}" />
                        </items>
                    </Select>

                    <Label text="{i18n>DEBITORENNUMMER}" />
                    <Input id="debitorennummer" value="{form>/vermietungsaktivitaet/Debitor}" editable="{= (${form>/modus} === 'new') || (${form>/modus} === 'edit') }" />
                    <Button text="{i18n>DEBITOR_AUSWAEHLEN}" visible="{= (${form>/modus} === 'new') || (${form>/modus} === 'edit') }" press="onDebitorAuswahlButtonPress" >
                        <layoutData>
                            <l:GridData span="L3 M4 S4" />
                        </layoutData>
                    </Button>

                    <Label text="{i18n>MIETERNAME}" required="{= (${form>/modus} === 'new') || (${form>/modus} === 'edit') }" />
                    <Input id="mietername" value="{form>/vermietungsaktivitaet/Debitorname}" editable="{= (${form>/modus} === 'new') || (${form>/modus} === 'edit') }" />

                    <Label text="{i18n>MIETBEGINN}" required="{= (${form>/modus} === 'new') || (${form>/modus} === 'edit') }" />
                    <DatePicker id="dateMietbeginn" dateValue="{form>/vermietungsaktivitaet/Mietbeginn}" placeholder=" " displayFormat="short" editable="{= (${form>/modus} === 'new') || (${form>/modus} === 'edit') }" />

                    <Label text="{i18n>LAUFZEIT_BIS_1ST_BREAK} {i18n>IN_MONATEN}" required="{= (${form>/modus} === 'new') || (${form>/modus} === 'edit') }" />
                    <Input id="laufzeitBis1stBreak" value="{form>/vermietungsaktivitaet/LzFirstbreak}" type="Number" editable="{= (${form>/modus} === 'new') || (${form>/modus} === 'edit') }" class="sapUiSmallMarginBottom" />                        
                    
                    <Label text="{i18n>MIETFREIE_ZEIT} {i18n>IN_MONATEN}" />
                    <Input id="mietfreieZeitenInMonaten" value="{form>/vermietungsaktivitaet/MzMonate}" type="Number" editable="{= (${form>/modus} === 'new') || (${form>/modus} === 'edit') }" />

                    <Label text="{i18n>ERSTER_MONAT_MIETFREI}" />
                    <DatePicker id="idMzErsterMonat" dateValue="{form>/vermietungsaktivitaet/MzErsterMonat}" placeholder=" " displayFormat="MM/yyyy" editable="{= (${form>/modus} === 'new') || (${form>/modus} === 'edit') }" />
                    
                    <Label text="{i18n>ANZAHL_WIEDERHOLUNGEN} {i18n>IN_JAHREN}" />
                    <Input id="idMzAnzahlJ" value="{form>/vermietungsaktivitaet/MzAnzahlJ}" type="Number" editable="{= (${form>/modus} === 'new') || (${form>/modus} === 'edit') }" class="sapUiSmallMarginBottom" />

                    <Label text="{i18n>MAKLERKOSTEN} {i18n>IN_MONATSMIETEN}" />
                    <Input id="maklerkostenInMonatsmieten" value="{form>/vermietungsaktivitaet/MkMonate}" type="Number" editable="{= (${form>/modus} === 'new') || (${form>/modus} === 'edit') }" />
                    
                    <Label text="{i18n>MAKLERKOSTEN} ({i18n>ABSOLUT}) in {form>/viewsettings/waehrungSelected/text}" />
                    <Input id="maklerkostenAbsolut" value="{form>/vermietungsaktivitaet/MkAbsolut}" type="Number" editable="{= (${form>/modus} === 'new') || (${form>/modus} === 'edit') }" class="sapUiSmallMarginBottom" />

                    <Label text="{i18n>BERATUNGSKOSTEN} {i18n>IN_MONATSMIETEN}" />
                    <Input id="beratungskostenInMonatsmieten" value="{form>/vermietungsaktivitaet/BkMonate}" type="Number" editable="{= (${form>/modus} === 'new') || (${form>/modus} === 'edit') }" />
                
                    <Label text="{i18n>BERATUNGSKOSTEN} ({i18n>ABSOLUT}) in {form>/viewsettings/waehrungSelected/text}" />
                    <Input id="beratungskostenAbsolut" value="{form>/vermietungsaktivitaet/BkAbsolut}" type="Number" editable="{= (${form>/modus} === 'new') || (${form>/modus} === 'edit') }" class="sapUiSmallMarginBottom" />

                    <!-- Sonstige Kosten -->
                    <Label text="{i18n>SONSTIGE_KOSTEN} in {form>/viewsettings/waehrungSelected/text}" />
                    <Select enabled="{= (${form>/modus} === 'new') || (${form>/modus} === 'edit') }" selectedKey="{form>/vermietungsaktivitaet/ArtKosten}">
                        <items>
                            <core:Item key="00" text="Sonstige Kostenart 00" />
                            <core:Item key="01" text="Sonstige Kostenart 01" />
                            <core:Item key="02" text="Sonstige Kostenart 02" />
                        </items>
                        <layoutData>
							<l:GridData span="L3 M3 S12" />
						</layoutData>
                    </Select>
                    <Input id="idSonstK" value="{form>/vermietungsaktivitaet/SonstK}" type="Number" editable="{= (${form>/modus} === 'new') || (${form>/modus} === 'edit') }">
                        <layoutData>
							<l:GridData span="L6 M6 S12" />
						</layoutData>
                    </Input>

                    <!-- Sonstige Erträge  -->
                    <Label text="{i18n>SONSTIGE_ERTRAEGE} in {form>/viewsettings/waehrungSelected/text}" />
                    <Select enabled="{= (${form>/modus} === 'new') || (${form>/modus} === 'edit') }" selectedKey="{form>/vermietungsaktivitaet/ArtErtrag}">
                        <items>
                            <core:Item key="00" text="Sonstige Ertragsart 00" />
                            <core:Item key="01" text="Sonstige Ertragsart 01" />
                            <core:Item key="02" text="Sonstige Ertragsart 02" />
                        </items>
                        <layoutData>
							<l:GridData span="L3 M3 S12" />
						</layoutData>
                    </Select>
                    <Input id="idSonstE" value="{form>/vermietungsaktivitaet/SonstE}" type="Number" editable="{= (${form>/modus} === 'new') || (${form>/modus} === 'edit') }" class="sapUiSmallMarginBottom">
                        <layoutData>
							<l:GridData span="L6 M6 S12" />
						</layoutData>
                    </Input>

                    <Label text="{i18n>ERSTER_MONAT_VERTEILUNG_AUSBAUKOSTEN}" />
                    <DatePicker id="idAkErsterMonat" dateValue="{form>/vermietungsaktivitaet/AkErsterMonat}" placeholder=" " displayFormat="MM/yyyy" editable="{= (${form>/modus} === 'new') || (${form>/modus} === 'edit') }" />

                    <Label text="{i18n>VERTEILUNG_AUSBAUKOSTEN_MONATE}" />
                    <Input id="idAkAnzahlM" value="{form>/vermietungsaktivitaet/AkAnzahlM}" type="Number" editable="{= (${form>/modus} === 'new') || (${form>/modus} === 'edit') }" class="sapUiSmallMarginBottom" />

                    <Label text="{i18n>POENALE} in {form>/viewsettings/waehrungSelected/text}" />
                    <Input id="poenale" value="{form>/vermietungsaktivitaet/Poenale}" type="Number" editable="{= (${form>/modus} === 'new') || (${form>/modus} === 'edit') }" />

                    <Label text="{i18n>INDEXWEITERGABE} {i18n>IN_PROZENT}" required="{= (${form>/modus} === 'new') || (${form>/modus} === 'edit') }" />
                    <Input id="indexweitergabe" value="{form>/vermietungsaktivitaet/IdxWeitergabe}" type="Number" editable="{= (${form>/modus} === 'new') || (${form>/modus} === 'edit') }" />

                    <Label text="{i18n>PLANUNGSRELEVANZ}" />
                    <CheckBox id="planungsrelevanz" selected="{form>/vermietungsaktivitaet/PLRelevant}" editable="{= (${form>/modus} === 'new') || (${form>/modus} === 'edit') }" />
                    
                </f:content>

			</f:SimpleForm>

            <f:SimpleForm
                title="{i18n>SONSTIGE_ANGABEN}"
                minWidth="1024"
                maxContainerCols="2"
                editable="true"
                layout="ResponsiveGridLayout"
                labelSpanL="3"
                labelSpanM="3"
                emptySpanL="0"
                emptySpanM="0"
                columnsL="1"
                columnsM="1" >

                <f:content>
                    
                    <Label text="{i18n>STATUS}" />
                    <Select id="selectStatus" selectedKey="{form>/vermietungsaktivitaet/Status}" width="100%" enabled="{= (${form>/modus} === 'new') || (${form>/modus} === 'edit') }" items="{form>/statuswerte}" change="onStatusSelektionChange" >
                        <items>
                            <core:Item key="{form>key}" text="{form>text}" />
                        </items>
                    </Select>
                    
                    <Label text="{i18n>ANMERKUNG}" />
                    <Select selectedKey="{form>/vermietungsaktivitaet/Anmerkung}" width="100%" enabled="{= (${form>/modus} === 'new') || (${form>/modus} === 'edit') }" items="{form>/anmerkungen}" >
                        <items>
                            <core:Item key="{form>key}" text="{form>text}" />
                        </items>
                    </Select>
                    
                    <Label text="{i18n>BEMERKUNG}" />
                    <TextArea value="{form>/vermietungsaktivitaet/Bemerkung}" rows="4" editable="{= (${form>/modus} === 'new') || (${form>/modus} === 'edit') }" />
                    
                    <Label text="{i18n>BUDGET_STOPP}" />
					<CheckBox id="budgetStopp" text="" selected="{form>/vermietungsaktivitaet/BudgetStopp}" editable="false">
                        <layoutData>
							<l:GridData span="L1 M1 S12" />
						</layoutData>
                    </CheckBox>

                </f:content>

			</f:SimpleForm>

            <Table>
                <headerToolbar>
                    <Toolbar>
                        <Title text="{i18n>MIETE_UND_KOSTEN}" level="H2"/>
                    </Toolbar>
                </headerToolbar>
                
                <columns>
                    <Column minScreenWidth="Tablet" demandPopin="true">
                        <Text text="{i18n>BEZEICHNUNG}" />
                    </Column>
                    <Column minScreenWidth="Tablet" demandPopin="true" hAlign="Right">
                        <Text text="{i18n>VERMIETUNGSAKTIVITAET}" />
                    </Column>
                    <Column minScreenWidth="Tablet" demandPopin="true" hAlign="Right">
                        <Text text="{i18n>KONDITIONENEINIGUNG}" />
                    </Column>
                    <Column minScreenWidth="Tablet" demandPopin="true" hAlign="Right">
                        <Text text="{i18n>DIFFERENZ}" />
                    </Column>
                </columns>
                
                <items>
                    <ColumnListItem>
                        <cells>
                            <Text text="{i18n>MIETE_GESAMT}" />
                            <Text text="{form>/vermietungsaktivitaet/mieteGesamt/vermietungsaktivitaet}" />
                            <Text text="{form>/vermietungsaktivitaet/mieteGesamt/konditioneneinigung}" />
                            <Text text="{form>/vermietungsaktivitaet/mieteGesamt/differenz}" />
                        </cells>
                    </ColumnListItem>
                    
                    <ColumnListItem>
                        <cells>
                            <Text text="{i18n>KOSTEN_GESAMT}" />
                            <Text text="{form>/vermietungsaktivitaet/kostenGesamt/vermietungsaktivitaet}" />
                            <Text text="{form>/vermietungsaktivitaet/kostenGesamt/konditioneneinigung}" />
                            <Text text="{form>/vermietungsaktivitaet/kostenGesamt/differenz}" />
                        </cells>
                    </ColumnListItem>
                </items>
            </Table>

		</content>
        
        <footer>
			<OverflowToolbar>
				<Button text="{= ${form>/vermietungsaktivitaet/Favorit} ? ${i18n>FAVORIT_ENTFERNEN} : ${i18n>FAVORIT_HINZUFUEGEN} }" icon="{= ${form>/vermietungsaktivitaet/Favorit} ? 'sap-icon://favorite' : 'sap-icon://unfavorite' }" press="onFavoritButtonPress" visible="{= (${form>/modus} === 'show') }" />
				<Button text="{i18n>GENEHMIGEN}" icon="sap-icon://accept" visible="{= (${form>/modus} === 'show') }" />
				<Button text="{i18n>NICHTGENEHMIGEN}" icon="sap-icon://decline" visible="{= (${form>/modus} === 'show') }" />
                <Button text="{i18n>DRUCKEN}" icon="sap-icon://print" press="onDruckenButtonPress" visible="{= (${form>/modus} === 'show') }" />
                <ToolbarSpacer visible="{= (${form>/modus} === 'new') || (${form>/modus} === 'edit') }" />
                <Button visible="{= (${form>/modus} === 'new') || (${form>/modus} === 'edit') }" text="{i18n>SPEICHERN}" icon="sap-icon://accept" type="Accept" press="onSpeichernButtonPress" >
                    <layoutData><OverflowToolbarLayoutData moveToOverflow="false" /></layoutData>
                </Button>
                <Button visible="{= (${form>/modus} === 'new') || (${form>/modus} === 'edit') }" text="{i18n>ABBRECHEN}" icon="sap-icon://decline" type="Reject" press="onAbbrechenButtonPress" >
                    <layoutData><OverflowToolbarLayoutData moveToOverflow="false" /></layoutData>
                </Button>
			</OverflowToolbar>
		</footer>

	</Page>
	
</core:View>