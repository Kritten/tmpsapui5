<mvc:View
    controllerName="ag.bpc.Deka.controller.KonditioneneinigungGenehmigung"
    xmlns="sap.m"
    xmlns:f="sap.ui.layout.form"
    xmlns:l="sap.ui.layout"
    xmlns:core="sap.ui.core"
    xmlns:mvc="sap.ui.core.mvc">
    
	<Page 
        id="idKonditioneneinigungGenehmigung"
        title="Konditioneneinigung Genehmigung" 
        class="sapUiFioriObjectPage" 
        showNavButton="{= ${form>/modus} === 'show' }"
        navButtonPress="onBack">

        <headerContent>
			<Button icon="sap-icon://edit" text="{i18n>BEARBEITEN}" press="onBearbeitenButtonPress" visible="{= ${form>/modus} === 'show' }" />
		</headerContent>

        <content>

            <List id="stufenList" items="{ path: 'form>/stufen', templateShareable: true }" backgroundDesign="Transparent" showSeparators="None">
                <CustomListItem>

                    <Table items="{ path: 'form>genehmigungen', templateShareable: true }" inset="true">

                        <headerToolbar>
                            <Toolbar>
                                <Title text="{= (${form>Stufe} === 'SB') ? ${i18n>SB} : 
                                                (${form>Stufe} === 'GL') ? ${i18n>GL} :
                                                (${form>Stufe} === 'AL') ? ${i18n>AL} :
                                                (${form>Stufe} === 'GS') ? ${i18n>GS} :
                                                (${form>Stufe} === 'BL') ? ${i18n>BL} :
                                                ${form>Stufe}}" />
                            </Toolbar>
                        </headerToolbar>

                        <columns>
                            <Column width="20%">
                                <Text text="Genehmiger" />
                            </Column>
                            <Column width="40%">
                                <Text text="Neuer Genehmiger" />
                            </Column>
                            <Column width="40%">
                                <Text text="Status" />
                            </Column>
                        </columns>

                        <items>
                            <ColumnListItem vAlign="Middle"> 
                                <Text text="{form>Genehmiger}">
                                    <layoutData>
                                        <l:GridData span="L8 M8 S12" />
                                    </layoutData>
                                </Text>                         
                                <Select items="{ path: 'form>available', templateShareable: true }" forceSelection="false" selectedKey="{path: 'form>newKey'}" enabled="{= (${form>Status} === 'Status 1' &amp;&amp; ${form>/modus} === 'edit')}" autoAdjustWidth="false" width="100%">
                                    <core:Item key="{form>Moegliche_Genehmiger}" text="{form>Moegliche_Genehmiger} - {form>Name}" />
                                    <layoutData>
                                        <l:GridData span="L8 M8 S12" />
                                    </layoutData>
                                </Select>                                
                                <Text text="{= ${text>/status}[${form>Status}]}"/>
                                
                            </ColumnListItem>
                        </items>

                    </Table>

                </CustomListItem>
            </List>

        </content>
        
        <footer>
            <OverflowToolbar>
                <ToolbarSpacer />
                <Button text="{i18n>SPEICHERN}" icon="sap-icon://accept" type="Accept" press="onSpeichernButtonPress" visible="{= ${form>/modus} === 'edit' }" >
                    <layoutData><OverflowToolbarLayoutData moveToOverflow="false" /></layoutData>
                </Button>
                <Button text="{i18n>ABBRECHEN}" icon="sap-icon://decline" type="Reject" press="onAbbrechenButtonPress" visible="{= ${form>/modus} === 'edit' }" >
                    <layoutData><OverflowToolbarLayoutData moveToOverflow="false" /></layoutData>
                </Button>
            </OverflowToolbar>
        </footer>

	</Page>
            
</mvc:View>